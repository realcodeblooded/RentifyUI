import 'package:flutter/material.dart';

class UnitsTab extends StatelessWidget {
  const UnitsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Units Placeholder'));
  }
}
