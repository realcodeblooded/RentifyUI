import 'package:flutter/material.dart';

class VacateTab extends StatelessWidget {
  const VacateTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Vacate Placeholder'));
  }
}
